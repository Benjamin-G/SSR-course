import React from 'react'

const Home = () => {
  return (
    <div>
      <div>I'm the BEST home component This is now working with live reload</div>
      <button onClick={() => console.log('Hi there!')}>Press me!</button>
    </div>
  )
}

export default Home